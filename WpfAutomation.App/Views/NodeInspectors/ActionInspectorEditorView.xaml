<UserControl x:Class="WpfAutomation.App.Views.NodeInspectors.ActionInspectorEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WpfAutomation.App.NodeInspector.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="380"
             d:DesignWidth="340">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Border Style="{StaticResource NodeInspector.CategoryContainerStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource NodeInspector.CategoryHeaderTextStyle}" Text="{Binding CategoryName}" />
                <TextBlock Style="{StaticResource NodeInspector.HelpTextStyle}" Text="{Binding HintText}" />
                <TextBlock Style="{StaticResource NodeInspector.HelpTextStyle}" Text="{Binding WarningText}"
                           Foreground="{StaticResource NodeInspector.ValidationBrush}" />

                <ItemsControl ItemsSource="{Binding Fields}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:InspectorFieldViewModel}">
                            <StackPanel Margin="0,0,0,8">
                                <TextBlock Style="{StaticResource NodeInspector.RowLabelTextStyle}" Text="{Binding Label}" />

                                <TextBox Text="{Binding StringValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource NodeInspector.TextBoxStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Kind}" Value="{x:Static vm:InspectorFieldKind.Text}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="{x:Static vm:InspectorFieldKind.Number}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <ComboBox ItemsSource="{Binding Choices}"
                                          SelectedItem="{Binding SelectedChoice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox" BasedOn="{StaticResource NodeInspector.ComboBoxStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Kind}" Value="{x:Static vm:InspectorFieldKind.Choice}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ComboBox.Style>
                                </ComboBox>

                                <CheckBox Content="Enabled"
                                          IsChecked="{Binding BoolValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox" BasedOn="{StaticResource NodeInspector.CheckBoxStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Kind}" Value="{x:Static vm:InspectorFieldKind.Toggle}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Margin="0,8,0,0"
                        HorizontalAlignment="Left"
                        Padding="10,4"
                        Command="{Binding ResetToDefaultsCommand}"
                        Content="Reset to Defaults"
                        Style="{StaticResource NodeInspector.SecondaryButtonStyle}" />

                <Border Style="{StaticResource NodeInspector.ValidationBannerStyle}"
                        Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl ItemsSource="{Binding ValidationErrors}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Foreground="{StaticResource NodeInspector.ValidationBrush}" Text="{Binding}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
